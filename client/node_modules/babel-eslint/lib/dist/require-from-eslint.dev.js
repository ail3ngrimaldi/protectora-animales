"use strict";

var resolve = require("resolve");

var eslintBase = require.resolve("eslint");

module.exports = function requireFromESLint(id) {
  var path = resolve.sync(id, {
    basedir: eslintBase
  });
  return require(path);
};