"use strict";

var _require = require("chai"),
    assert = _require.assert;

var _require2 = require("../lib"),
    decode = _require2.decode,
    encode = _require2.encode;

describe("UTF8", function () {
  it("should f-1(f(x)) = x", function () {
    assert.equal(decode(encode("foo")), "foo");
    assert.equal(decode(encode("éé")), "éé"); // TODO(sven): utf8 encoder fails here
    // assert.equal(decode(encode("🤣见見")), "🤣见見");
  });
});