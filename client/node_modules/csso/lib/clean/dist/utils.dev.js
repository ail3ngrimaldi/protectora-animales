"use strict";

module.exports = {
  hasNoChildren: function hasNoChildren(node) {
    return !node || !node.children || node.children.isEmpty();
  },
  isNodeChildrenList: function isNodeChildrenList(node, list) {
    return node !== null && node.children === list;
  }
};