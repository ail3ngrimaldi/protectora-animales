"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});

var experimental_utils_1 = require("@typescript-eslint/experimental-utils"); // note - cannot migrate this to an import statement because it will make TSC copy the package.json to the dist folder


var version = require('../../package.json').version;

exports.createRule = experimental_utils_1.ESLintUtils.RuleCreator(function (name) {
  return "https://github.com/typescript-eslint/typescript-eslint/blob/v".concat(version, "/packages/eslint-plugin/docs/rules/").concat(name, ".md");
});