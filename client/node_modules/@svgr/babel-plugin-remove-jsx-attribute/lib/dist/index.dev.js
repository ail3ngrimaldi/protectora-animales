"use strict";

exports.__esModule = true;
exports["default"] = void 0;

var removeJSXAttribute = function removeJSXAttribute(api, opts) {
  return {
    visitor: {
      JSXOpeningElement: function JSXOpeningElement(path) {
        if (!opts.elements.includes(path.node.name.name)) return;
        path.get('attributes').forEach(function (attribute) {
          var nodeName = attribute.node.name;

          if (nodeName && opts.attributes.includes(nodeName.name)) {
            attribute.remove();
          }
        });
      }
    }
  };
};

var _default = removeJSXAttribute;
exports["default"] = _default;